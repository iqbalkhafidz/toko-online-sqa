name: SQA CI & CT Pipeline

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          coverage: xdebug
          tools: composer:v2

      - name: Install PHP Dependencies
        run: composer install --prefer-dist --no-progress --ignore-platform-req=php

      - name: Run PHPUnit & Quality Metrics
        # Perubahan utama: Menggunakan filter pencarian class 'Test' secara eksplisit
        # Dan membatasi scope scan agar file inisialisasi bermasalah tidak ikut dieksekusi sebagai test
        run: |
          export XDEBUG_MODE=coverage
          php vendor/bin/phpunit --bootstrap vendor/autoload.php --filter=Test tests/ --coverage-text --coverage-filter src/

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install NPM Dependencies
        run: npm install

      - name: Start PHP Server
        run: php -S localhost:8000 > /dev/null 2>&1 &

      - name: Run Cypress E2E Tests
        uses: cypress-io/github-action@v6
        with:
          wait-on: 'http://localhost:8000'
          browser: chrome